{
  "name": "@hyperspace-zone/control-plane-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {
    "@hyperspace-zone/contracts": "0.1.0",
    "@hyperspace-zone/db": "0.1.0",
    "@hyperspace-zone/shared": "0.1.0",
    "fastify": "^5.6.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
