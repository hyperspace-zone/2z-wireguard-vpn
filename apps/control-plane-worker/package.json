{
  "name": "@hyperspace-zone/control-plane-worker",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@hyperspace-zone/control-plane": "0.1.0",
    "@hyperspace-zone/contracts": "0.1.0",
    "@hyperspace-zone/db": "0.1.0",
    "@hyperspace-zone/shared": "0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
